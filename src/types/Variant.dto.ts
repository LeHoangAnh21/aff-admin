export interface Variant {
    id?: string,
    title?: string,
    // stock?: any,
    description?: string
    price?: number,
    byService?: boolean,
    serial?: any,
    service?: string,
    quantity?: number

}